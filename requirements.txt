# Core: analysis + training (analysis/evaluate.py, ml/*.py)
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0
matplotlib>=3.6.0      # plots in analysis/evaluate.py (degrades gracefully if absent)

# Telemetry collection (telemetry/scanner.py) — needed only to gather new data
dnspython>=2.3.0

# Go model export (ml/trainer.py) — needed only to regenerate engine/rf_model.go
m2cgen>=0.10.0
